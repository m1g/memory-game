import React, { Component } from 'react'
import characters from '../characters.json'

class Cards extends Component {

  render () {
    const cards = this.state.cards.map(2 * (card, i) => {
      const isFlipped = card.flip ? styles.flip : ''
      return
    })
}

export default Cards
